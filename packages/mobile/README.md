# packages/mobile

Not yet implemented — scaffolded as part of Phase 3 (see [docs/App-dev.md](../../docs/App-dev.md) §7, §10).

Planned: Expo/React Native app using `react-native-track-player` for background audio and lock-screen controls, consuming `@quran-fm/core` for stream resolution and i18n — same as `packages/web`.
