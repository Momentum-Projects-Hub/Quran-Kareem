# src-tauri

Not yet implemented — scaffolded as part of Phase 2 (see [docs/App-dev.md](../docs/App-dev.md) §5, §10).

Planned: Tauri shell wrapping the `packages/web` build output (`devPath`/`distDir` pointed at it), with a system-tray play/pause toggle as a stretch goal.
